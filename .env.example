# Optional. Without a key the semantic gate falls back to deterministic
# heuristics, so the checks still mean something on fork PRs and offline.
OPENAI_API_KEY=

# Optional: which OpenAI model judges contracts. Defaults inside the script.
# CM_JUDGE_MODEL=gpt-5.1
