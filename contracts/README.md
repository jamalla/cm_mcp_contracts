# The approved lane

Approved Salla tool contracts land here, one JSON file per contract, each
referencing `../schema/tool-contract.v1.json` via its `$schema` key.

The directory is empty until the first contracts merge through the gate --
git needs this file to keep the directory in a fresh checkout.
